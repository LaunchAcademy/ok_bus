<% if @review.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@review.errors.count, "error") %> prohibited this review from being saved:</h2>

    <ul>
    <% @review.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
    </ul>
  </div>
<% end %>

<%= form_for([@bus, @ride]) do |f| %>
    <%= f.label :direction %>
    <%= f.collection_select(:ride, :direction, @bus.rides, :direction) %>
    <%= f.label :day %>
    <%= f.collection_select(:ride, :day, @bus.rides, :day) %>

    <%= f.label :rating %><br>
    <%= f.select :rating, (1..5), include_blank: true %>

  <div class="actions">
    <%= f.submit 'Create Review'%>
  </div>
<% end %>

<%= link_to 'Back', bus_path(@bus) %>
