<%= form_for(@ride, @review) do |f| %>
 <% if @review.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@review.errors.count, "error") %> prohibited this review from being saved:</h2>

      <ul>
      <% @review.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :bus %><br>
    <%= f.collection_select :bus_id, Bus.all, :id, :number %>
  </div>
  <div class="field">
    <%= f.label :direciton %><br>
    <%= f.collection_select :ride_id, Ride.all, :id, :direction %>
  </div>
  <div class="field">
    <%= f.label :day %><br>
    <%= f.collection_select :ride_id, Ride.all, :id, :day %>
  </div>
  <div class="field">
    <%= f.label :timeframe %><br>
    <%= f.collection_select :ride_id, Ride.all, :id, :timeframe %>
  </div>
  <div class="field">
    <%= f.label :rating %><br>
    <%= f.select :rating, (1..5) %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

<%= link_to 'Back', questions_path %>
