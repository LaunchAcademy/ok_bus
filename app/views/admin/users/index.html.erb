<h4>User Index</h4>
<ul>
  <% @users.each do |user|%>
    <li><%= user.email %> <%= image_tag gravatar_url(user) %> <%= link_to "Delete", admin_user_path(user), method: :delete %></li>
  <% end %>
</ul>
