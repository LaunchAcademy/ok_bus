<% @reviews.each do |review| %>
  <div class="panel">
  <li id="<%= "review-#{review.id}" %>">
    <div class="row">
      <div class="small-2 column">
        <ul class="no-bullet">
          <li>
            <a class="clearing-thumbs">
            <img src=<%= review.user.profile_photo %>>
            </a>
          </li>
        </ul>
        <ul class="no-bullet">
          <% if user_signed_in? %>
            <li class="upvote">
              <%= change_or_vote(review.ride.bus, review, "Up") %>
              <%= review.up_votes %> votes
            </li>
            <li class="downvote">
              <%= change_or_vote(review.ride.bus, review, "Down") %>
              <%= review.down_votes %> votes
            </li>
            <li>
              <%= delete_vote(review.ride.bus, review) %>
            </li>
          <% end %>
        </ul>
      </div>
      <div class="small-8 column">
        <p>
          <%= review.user.username.upcase %>
          rode bus <%= review.ride.bus.number %>
          <%= review.ride.direction %>
          on a <%= review.ride.day %>
          between <%= review.ride.timeframe %>
        </p>
        <p><%= review.rating %> out of 5</p>
        <p><%= review.body %></p>
      </div>
      <div class="small-2 column">
        <ul class="no-bullet">
          <li>
            <% if current_user && (current_user == review.user || current_user.admin?) %>
              <%= link_to "Edit", edit_bus_review_path(review.ride.bus, review) %>
               | <%= link_to 'Delete',
                  review_path(review),
                  method: :delete,
                  data: { confirm:
                          'Are you sure?'
                          } %>
            <% end %>
          </li>
        </ul>
      </div>
    </div>
    </li>
  </div>
<% end %>

